{
  "entities": {
    "Trip": {
      "title": "Trip",
      "description": "Collaborative expedition plan with activities, schedule, gear, chaperones, and student rosters",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique trip plan identifier",
          "maxLength": 128
        },
        "tripName": {
          "type": "string",
          "description": "Name of the expedition or field trip",
          "maxLength": 150
        },
        "destination": {
          "type": "string",
          "description": "Destination name, campground, or address",
          "maxLength": 200
        },
        "startDate": {
          "type": "string",
          "description": "Start date in YYYY-MM-DD format",
          "maxLength": 30
        },
        "endDate": {
          "type": "string",
          "description": "End date in YYYY-MM-DD format",
          "maxLength": 30
        },
        "expectedStudents": {
          "type": "number",
          "description": "Total number of anticipated student attendees"
        },
        "days": {
          "type": "array",
          "description": "List of day plans with timeline slots and themes"
        },
        "activities": {
          "type": "array",
          "description": "List of scheduled and bank activities"
        },
        "supplies": {
          "type": "array",
          "description": "Master equipment and supplies checklist"
        },
        "studentItems": {
          "type": "array",
          "description": "Required student packing gear items"
        },
        "teachers": {
          "type": "array",
          "description": "Lead coordinators and teacher chaperone assignments"
        },
        "milestones": {
          "type": "array",
          "description": "Pre-trip preparation tasks and deadlines"
        },
        "studentGroups": {
          "type": "array",
          "description": "Cohort waves and groups (e.g. Group 1, Group 2)"
        },
        "students": {
          "type": "array",
          "description": "Student attendees, houses, and medical dietary requirements"
        },
        "lastSavedAt": {
          "type": "string",
          "description": "ISO timestamp of last live edit",
          "maxLength": 50
        },
        "updatedBy": {
          "type": "string",
          "description": "Display name or email of user who made the last change",
          "maxLength": 100
        }
      },
      "required": ["id", "tripName", "startDate", "endDate", "days"]
    }
  },
  "firestore": {
    "/trips/{tripId}": {
      "schema": "Trip",
      "description": "Live collaborative trip projects shared across all coordinators"
    }
  }
}
